@model IEnumerable<DataCenterSpecialist.Models.SFP>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Adicionar ", "Create")
</p>
<table class="table table-striped">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Fabricante)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Referencia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Quantidade)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Fabricante)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Referencia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Quantidade)
        </td>
        <td>
            <a href="#"><span class="glyphicon glyphicon-remo"></span></a>
            @Html.ActionLink(" ", "Edit", new { id=item.SFPID }, new { @class = "text-info glyphicon glyphicon-pencil" }) |
            @Html.ActionLink(" ", "Details", new { id=item.SFPID }, new { @class = "text-success glyphicon glyphicon-eye-open" }) |
            @Html.ActionLink(" ", "Delete", new { id=item.SFPID }, new { @class = "text-danger glyphicon glyphicon-remove" })
        </td>
    </tr>
}

</table>
